apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook
spec:
  replicas: 7                     # Self-heal demo-ku idha use pannuvom
  selector:
    matchLabels:
      app: guestbook-ui
  template:
    metadata:
      labels:
        app: guestbook-ui
    spec:
      containers:
      - name: guestbook
        image: nginx:latest
        ports:
        - containerPort: 89
